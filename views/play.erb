<%= @game.current_player.name %> vs. <%= @game.opposing_player.name %>

<%= @game.opposing_player.name %>: <%= @game.opposing_player.hit_points %>HP
<%= @game.current_player.name %>: <%= @game.current_player.hit_points %>HP

<form action="/attack">
  <input type="submit" value="Attack">
</form>
